# 🚀 봇 통합관리 시스템 서버 설치 및 운용 가이드

이 문서는 로컬 환경에서 개발된 봇들을 리눅스(Ubuntu) 서버로 이전하고, PM2를 사용하여 안정적으로 운영하는 방법을 설명합니다.

---

## **1. 서버 준비 및 접속**
1. **Ubuntu 설치**: 서버에 우분투를 설치하고 인터넷 연결을 확인합니다.
2. **VS Code SSH 설정**: 
   - VS Code에서 `Remote - SSH` 확장을 설치합니다.
   - 서버의 IP와 계정 정보를 입력하여 원격 접속을 완료합니다.

## **2. 필수 소프트웨어 설치**
서버 터미널에서 아래 명령어를 순서대로 입력하여 필요한 환경을 구축합니다.

```bash
# 시스템 업데이트
sudo apt update && sudo apt upgrade -y

# Python 및 가상환경 도구 설치
sudo apt install python3-pip python3-venv -y

# Node.js 및 PM2 설치 (프로세스 관리용)
sudo apt install nodejs npm -y
sudo npm install pm2 -g
```

## **3. 프로젝트 복사 및 설정**
1. **파일 업로드**: VS Code를 통해 `봇 통합관리` 폴더 전체를 서버로 복사합니다.
   - *주의: 기존 `venv` 폴더는 제외하고 복사하세요.*
2. **환경 변수 확인**: 각 봇 폴더의 `.env` 파일에 API Key가 올바르게 입력되었는지 확인합니다.

## **4. 파이썬 가상환경 및 라이브러리 설치**
```bash
cd "/path/to/봇 통합관리"
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

## **5. PM2를 이용한 봇 실행**

### **방법 A: 통합 관리자 실행 (권장)**
모든 봇과 웹 대시보드를 한 번에 관리합니다.
```bash
pm2 start ecosystem.config.js --only bot-integrated-manager
```

### **방법 B: 특정 봇만 개별 실행**
```bash
pm2 start ecosystem.config.js --only bot-30m-standalone
pm2 start ecosystem.config.js --only bot-1h-standalone
```

## **6. 주요 PM2 명령어**
- **상태 확인**: `pm2 status`
- **실시간 로그**: `pm2 logs bot-integrated-manager`
- **봇 재시작**: `pm2 restart bot-integrated-manager`
- **봇 중지**: `pm2 stop bot-integrated-manager`
- **서버 재부팅 시 자동 실행**:
  ```bash
  pm2 save
  pm2 startup
  ```

## **7. 대시보드 모니터링**
- 브라우저 주소창에 `http://서버IP:8000`을 입력하여 접속합니다.
- 서버의 방화벽(UFW 등)에서 8000번 포트가 허용되어 있어야 합니다.

---
**💡 Tip**: 서버 환경에서 `venv`를 새로 만드는 것이 호환성 문제 해결의 핵심입니다!
